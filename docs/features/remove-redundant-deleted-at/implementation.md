---
type: implementation
status: active
---
# Implementacion
Usar `replace_with_git_merge_diff` para remover `&& \w*\.DeletedAt == null` y expresiones similares como `.Where(\w* => \w*\.DeletedAt == null)` en los dos archivos.
