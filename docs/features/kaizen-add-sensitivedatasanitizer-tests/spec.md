# Specifications

Create `SensitiveDataSanitizerTests.cs` in `src/tests/GesFer.Product.UnitTests/Services/` using `xUnit` and `FluentAssertions` to test `GenerateRandomPassword`, `GenerateRandomEmail`, and `Sanitize` methods.