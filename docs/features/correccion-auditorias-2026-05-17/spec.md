# Specification
- **Search Perimeter**: Entire source code in `src/`.
- **Target**: Ensure zero compiler warnings and structural integrity.
- **Criteria**: No synchronous blocks on async tasks, no `async void`, no `TODO` markers.