---
name: HealthController XML Specs
---
# Specification
The `HealthController` needs its `Get` endpoint documented using `<returns>` tag and `[ProducesResponseType]` attribute for HTTP status 200 OK.