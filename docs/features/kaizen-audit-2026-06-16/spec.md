---
type: kaizen
---
# Specification
The current branch (`feat/audit-16624511735477172048-ad18a8af`) violates the strict branch nomenclature rules enforced by the CI script `validate-nomenclatura.ps1`. The specification requires renaming the branch to a compliant format such as `jules-<branch-name>`.
