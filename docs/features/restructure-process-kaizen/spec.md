---
feature: restructure-process-kaizen
status: planned
---

# Specification

According to the kaizen analysis file `SddIA/process/analisis-restructuracion-patron-skills-tools.md`, the `process` and `actions` items must follow the standard `skills` and `tools` patterns where every item has its own folder containing `spec.md` and `spec.json`.
For this single iteration, we migrate `create-pattern` and `create-principle`.
