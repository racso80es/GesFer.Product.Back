---
feature: restructure-process-kaizen
status: planned
---

# Objectives

- Move `create-pattern.md`, `create-pattern.json`, `create-principle.md`, and `create-principle.json` into their respective folders (`create-pattern/` and `create-principle/`) within `SddIA/process/`.
- Ensure each process has a `spec.md` and `spec.json`.
- Restructure SddIA tools structure as outlined in the kaizen task.
